version: '3.8'

services:
  web-local:
    build:
      context: .
      target: dev
    ports:
      - "3009:3009"
    restart: unless-stopped
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    environment:
      - PORT=3009
      - NODE_ENV=development
      - TZ=Asia/Ho_Chi_Minh
